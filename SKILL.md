---
name: daily-report
description: 当用户需要按日期生成、查询、创建、追加或修改个人日报，或把日报写入并最终提交到公司系统时使用；尤其适用于各阶段可能由同一工具或不同工具完成的场景。
---

# 日报编排

把工作流拆成三个独立阶段：日报存储端、公司系统写入端、最终提交端。公司系统写入端与最终提交端可能是 `same_tool`，也可能是 `separate_tools`；三个角色也可能位于同一运行端或分开的运行端。不要根据软件名称猜测能力、授权、字段映射或成功条件。配置字段见 [references/workflow-config.md](references/workflow-config.md)。

## 首次确认与持久化

先读取 `${DAILY_REPORT_CONFIG:-~/.config/daily-report/config.json}` 中的 `workflow`。只询问本次操作所缺少且会影响执行的决定；用户明确回答后立即持久化，后续不再重复询问。可以把相互关联的缺失项合并成一次简短提问，但不能用默认猜测代替用户决定。

按当前任务需要确认：

1. `workflow_mode`：角色位于 `same_runtime` 还是 `separate_runtimes`。
2. `local_roles`：当前运行端承担日报存储端、公司系统写入端、最终提交端中的哪些角色。
3. `company_action_mode`：写入与最终提交使用 `same_tool` 还是 `separate_tools`。
4. 各角色实际使用的工具、目标位置和可用的读取、写入、提交、回读能力。
5. 日报栏目到公司系统字段的 `field_mapping`，写入成功凭证与最终提交成功凭证。
6. 是否在写入成功后自动继续最终提交，以及最终提交成功后是否自动回写日报状态。
7. 当前阶段的持久授权；首次生成本周总结时，还要确认使用全部项目还是指定项目。

配置完整时直接复用。用户本次明确指定的日期、项目、工具或映射只覆盖本次，除非用户明确要求保存。旧配置中的中间服务地址或 Key 不参与当前流程，也不要自动删除。

配置只保存逻辑工具标识、目标、字段映射、成功条件、授权状态和偏好。实际密钥、令牌、密码只能保存在对应工具、环境变量或凭据管理器中；配置最多保存 `credential_ref`，不得保存秘密本身。配置目录权限为 `0700`，文件权限为 `0600`；更新时保留未知字段，原子写入后回读验证。

持久授权不覆盖高风险动作：删除、移动、批量替换、修改权限等仍按当前工具要求再次确认。工具要求严格安全写入时，执行预检，使用新的请求 ID 和服务端返回的前置条件，完成后按稳定路径或 ID 精确回读。

## 日期与日报存储

- 必须严格按日期执行，不得用相邻日期或最近一篇替代。
- 所有读取、创建、追加、修改、写入公司系统和最终提交都必须绑定明确的 `YYYY-MM-DD`。未给日期时先询问，不能用“最新”“最近”或其他日期代替。
- 默认时区为 `Asia/Shanghai`，默认思源路径为 `/工作/daily note/YYYY/MM/YYYY-MM-DD`，实际持久化配置优先。
- 只在已配置的个人日报笔记本和目标日期精确路径内查询，不跨笔记本搜索他人的日报，不得搜索或选择最新日报。
- 今天的日报可使用每日笔记创建动作；补写历史日报使用明确日期路径。创建、追加或修改前先读取完整目标，写入后精确回读。
- 内容是结果性总结，不是任务流水账。每条以项目名称开头；同一栏目有多条时分别使用 `1.`、`2.` 编号。保留完成事项、进行中、风险和后续动作等栏目。

## 本周项目与任务

第一次生成本周总结时询问“使用本周全部项目，还是指定项目？”。选择后写入 `project_scope`，以后自动复用，不再询问目录。用户临时指定项目时只覆盖本次。按 `Asia/Shanghai` 统计本周一 `00:00` 至当前时间，将同项目同状态事项合并成简明结果；不能验证的状态不猜测。

## 写入公司系统

读取精确日期日报后，按已确认的模板和 `field_mapping` 填写公司系统。写入成功必须取得 `write_success_evidence`，例如草稿记录 ID、已保存状态或精确回读结果。写入成功不等于最终提交成功。

如果写入结果失败、超时或不确定，先查询同日期、同目标记录，禁止重复填写。若确认已写入但尚未最终提交，可在获授权时回写“已写入待提交”及非敏感记录引用，不能写成“已提交”。

## 最终提交

当 `auto_submit_after_write=true`、当前授权允许且最终提交端可用时，写入验证成功后自动继续最终提交，不再重复询问。`same_tool` 仍要区分写入和提交两个阶段；`separate_tools` 通过已确认的 `handoff_mode` 传递日期、目标记录引用和必要的非敏感内容，不传递秘密。

最终提交必须取得 `submit_success_evidence`，例如提交记录 ID、明确成功状态或可回读的已提交结果。只有最终提交成功，才在同一日期的思源日报中追加或更新“已提交”标记，并回读验证。提交失败、超时或结果不确定时不写“已提交”，先查询下游状态；已有同日期、同目标系统的“已提交”记录时默认停止，不得重复提交。

## 阶段边界

- 缺少本阶段工具、目标、授权、字段映射或成功凭证定义时，只询问缺失项并停在该阶段，不执行猜测性的部分操作。
- 日报读取成功、公司系统写入成功、最终提交成功、思源状态回写成功是四个独立结果，分别验证并报告。
- 分开的运行端只执行 `local_roles` 中的阶段；完成后输出下一阶段所需的日期、非敏感正文或记录引用。不得声称另一个运行端已经执行。
- 任一阶段结果不确定时先精确回读，禁止换用另一日期或另一份“最新日报”，也禁止直接重试造成重复写入或重复提交。
